-- intitialize the empty folder with npm init  , it will create a package.json file
-- install a package ,  npm install mongodb
-- we have writter a CRUD operation on mongodb , using Node

-- We will create a web Api , so that applications can use our end points to show the data on the server

-- create  server.js